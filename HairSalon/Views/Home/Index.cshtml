@{
    ViewData["Title"] = "Home Page";
}

<div class="container min-vh-100 d-flex justify-content-center">
    <div class="text-center">
        <h1 class="display-4">Welcome to HairSalon</h1>
        <p class="lead">Manage your stylists and clients with ease.</p>
        <a class="btn btn-primary btn-lg" href="@Url.Action("Index", "Stylist")">View All Stylists</a>
    </div>
</div>
